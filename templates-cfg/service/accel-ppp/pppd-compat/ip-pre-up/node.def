type: txt
help: Path  to  ip-pre-up  script  which is executed before ppp interface comes up, useful to setup firewall rules before any traffic can pass through the interface.
syntax:expression: exec "[ -x $VAR(@) ]" ; "Script should exist and be executable by root."

